{
    "name": "productsdisplay-v2",
    "private": true,
    "type": "module",
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "scripts": {
        "format": "bun run format:biome",
        "format:biome": "biome check --write --linter-enabled=false .",
        "format:check": "biome check --linter-enabled=false .",
        "lint": "bun run lint:biome && bun run lint:markdown && bun run lint:yaml && bun run lint:web",
        "lint:biome": "biome lint apps/api packages/contracts scripts package.json apps/api/package.json apps/web/package.json apps/web/eslint.config.js biome.jsonc .markdownlint-cli2.jsonc",
        "lint:markdown": "markdownlint-cli2",
        "lint:web": "cd apps/web && bun run lint",
        "lint:yaml": "bun ./scripts/lint-yaml.ts"
    },
    "devDependencies": {
        "@biomejs/biome": "^2.3.2",
        "markdownlint-cli2": "^0.18.1",
        "yaml": "^2.8.1"
    }
}
